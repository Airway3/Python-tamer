num_lines = sum(1 for line in open('myfile.txt')) считает количество строк

d = [input().split(';') for i in range(n)] добавить в список d n раз списки, состоящие из разделённых символом ; элементов

coma,an,comb,bn = input().split(';') присвоить переменным вбивая им соответствующие значения разграничивая их символом ;

for i, item in enumerate(a):
    a[i] = int(item)  # список из строк в список из целых чисел

расширение = когда в переопределённом методе вызывается метод родителя + действие с ним
перекрытие = когда переопределён метод родителя и он не вызывается в теле этого метода

считается false (0 [] {} '' None)













 